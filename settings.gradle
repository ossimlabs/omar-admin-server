pluginManagement {
  repositories {
    maven { url "${ System.getenv('REPOSITORY_MANAGER_URL') }/ossim-deps" }
    gradlePluginPortal()
  }
}

include 'omar-admin-server'

project(':omar-admin-server').projectDir = "${rootProject.projectDir}/apps/omar-admin-server" as File
